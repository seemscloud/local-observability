apiVersion: 1

datasources:
  - name: Prometheus
    uid: observability-prometheus
    type: prometheus
    access: proxy
    url: http://observability-prometheus:9090
    isDefault: false
    editable: true
    jsonData:
      timeInterval: 15s
