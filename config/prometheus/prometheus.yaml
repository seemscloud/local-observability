global:
  scrape_interval: 5s
  scrape_timeout: 4s
  evaluation_interval: 5s

scrape_configs:
  - job_name: compute
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/compute.json
        refresh_interval: 1m
