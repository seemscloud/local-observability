global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

scrape_configs:
  - job_name: compute
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/compute.json
        refresh_interval: 1m
